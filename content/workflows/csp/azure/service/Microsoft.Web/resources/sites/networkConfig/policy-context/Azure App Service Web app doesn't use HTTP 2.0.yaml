schema: 1
type: policy-context
 
id: Azure App Service Web app doesn't use HTTP 2.0
name: Azure App Service Web app doesn't use HTTP 2.0
labels:
  - contextualization
 
csp: azure
service: Microsoft.Web
resource-type: sites/networkConfig
 
class: risk
subclass: config
category: visibility
subcategory: hygiene
 
filters:
  - vendor: prisma-cloud
    policies:
      - 4f5c4a28-c3df-4bee-a980-621c794548ed
    match-type: any
    rules:
      - .vendorId == "prisma-cloud" and .vendorPolicy == "4f5c4a28-c3df-4bee-a980-621c794548ed"
 
risk-config:
  default-risk: medium
